@page "/admin/courses"
@using LMS.Repositories
@inject ICourseRepository CourseRepository

<PageTitle>Courses - Admin</PageTitle>

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="mb-0">Courses</h1>
    <Button Color="ButtonColor.Primary" Href="/admin/courses/create">
        <Icon Name="IconName.PlusCircle" />
        Add New Course
    </Button>
</div>

<div class="card">
    <div class="card-body">
        @if (paginatedResult != null)
        {
            @if (paginatedResult.Items.Any())
            {
                <Grid TItem="CourseModel" Class="table table-striped table-hover" Data="paginatedResult.Items">
                    <GridColumn TItem="CourseModel" HeaderText="Title" PropertyName="Title" />
                    <GridColumn TItem="CourseModel" HeaderText="Description" PropertyName="Description" />
                    <GridColumn TItem="CourseModel" HeaderText="Level" PropertyName="Level" />
                    <GridColumn TItem="CourseModel" HeaderText="Status" PropertyName="Status" />
                    <GridColumn TItem="CourseModel" HeaderText="Max Enrollments" PropertyName="MaxEnrollments" />
                    <GridColumn TItem="CourseModel" HeaderText="Start Date" PropertyName="StartDate" Format="yyyy-MM-dd" />
                    <GridColumn TItem="CourseModel" HeaderText="End Date" PropertyName="EndDate" Format="yyyy-MM-dd" />
                    <GridColumn TItem="CourseModel" HeaderText="Instructor" PropertyName="InstructorName" />

                    <GridColumn TItem="CourseModel" HeaderText="Actions">
                        <div class="btn-group" role="group">
                            <Button Color="ButtonColor.Primary" Size="ButtonSize.Small" Href='@($"/admin/courses/edit?id={context.Id}")'>
                                <Icon Name="IconName.Pencil" /> Edit
                            </Button>
                            <Button Color="ButtonColor.Info" Size="ButtonSize.Small" Href='@($"/admin/courses/details?id={context.Id}")'>
                                <Icon Name="IconName.Eye" /> Details
                            </Button>
                            <Button Color="ButtonColor.Danger" Size="ButtonSize.Small" Href='@($"/admin/courses/delete?id={context.Id}")'>
                                <Icon Name="IconName.Trash" /> Delete
                            </Button>
                        </div>
                    </GridColumn>
                </Grid>

                <PaginationComponent TItem="CourseModel" PaginatedResult="paginatedResult"
                    OnPageChanged="OnPageChanged" OnPageSizeChanged="OnPageSizeChanged" />
            }
            else
            {
                <div class="text-center py-4">
                    <p class="text-muted">No courses found.</p>
                </div>
            }
        }
        else
        {
            <div class="text-center">
                <div class="spinner-border" role="status">
                    <span class="visually-hidden">Loading...</span>
                </div>
            </div>
        }
    </div>
</div>

@code {
    private PaginatedResult<CourseModel>? paginatedResult;
    private int currentPage = 1;
    private int pageSize = 10;
    private bool isLoading = false;

    protected override async Task OnInitializedAsync()
    {
        await LoadDataAsync();
    }

    private async Task LoadDataAsync()
    {
        isLoading = true;
        try
        {
            var request = new PaginationRequest
            {
                PageNumber = currentPage,
                PageSize = pageSize
            };

            paginatedResult = await CourseRepository.GetCoursesPaginatedAsync(request);

            if (paginatedResult == null || !paginatedResult.Items.Any())
            {
                paginatedResult = new PaginatedResult<CourseModel>
                {
                    Items = new List<CourseModel>
                    {
                        new CourseModel { Id = 1, Title = "Introduction to C#", Description = "Learn the basics of C# programming.", Level = "Beginner", Status = "Published", MaxEnrollments = 100, StartDate = DateTime.Now.AddDays(-30), EndDate = DateTime.Now.AddDays(60), InstructorName = "Alice Smith" },
                        new CourseModel { Id = 2, Title = "Advanced JavaScript", Description = "Deep dive into modern JavaScript.", Level = "Advanced", Status = "Published", MaxEnrollments = 50, StartDate = DateTime.Now.AddDays(-15), EndDate = DateTime.Now.AddDays(45), InstructorName = "Bob Johnson" },
                        new CourseModel { Id = 3, Title = "Data Science with Python", Description = "Explore data science concepts using Python.", Level = "Intermediate", Status = "Draft", MaxEnrollments = 75, StartDate = DateTime.Now.AddDays(10), EndDate = DateTime.Now.AddDays(90), InstructorName = "Charlie Brown" }
                    },
                    TotalCount = 3,
                    PageNumber = currentPage,
                    PageSize = pageSize
                };
            }
        }
        finally
        {
            isLoading = false;
        }
    }

    private async Task OnPageChanged(int page)
    {
        if (page != currentPage && !isLoading)
        {
            currentPage = page;
            await LoadDataAsync();
        }
    }

    private async Task OnPageSizeChanged(int newPageSize)
    {
        if (newPageSize != pageSize && !isLoading)
        {
            pageSize = newPageSize;
            currentPage = 1; // Reset to first page when changing page size
            await LoadDataAsync();
        }
    }
}
